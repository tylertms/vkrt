#version 460
#extension GL_EXT_ray_tracing : require

#include "utility/bindings.glsl"

void main() {
    if (scene.selectionEnabled == 0u || gl_InstanceCustomIndexEXT != scene.selectedMeshIndex) {
        ignoreIntersectionEXT;
    }
}
